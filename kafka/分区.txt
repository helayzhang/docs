
topic之间相互独立

每个topic可以分为多个partition（分区）

分区的作用：
	1. 提升处理能力（吞吐量）
		分区会被分布到不同的服务器上，每个服务器处理它分到的分区。这样可以增加处理能力，更多的服务器可以处理更多的数据。
	2. 容错备份
		分区可以配置复制到其他服务器进行备份容错，每个分区有一个leader，0或多个follower，leader处理此分区的所有读写请求，follower被动的复制数据。leader宕机其他一个follower会被推举为新的leader。
	3. 分区内串行，保证时序性；分区间并行，保证吞吐量