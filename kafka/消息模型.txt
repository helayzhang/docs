队列模型
	一组消费者从服务器读取消息，一条消息只有其中一个消费者来处理。（类似派发任务）

发布-订阅模型
	消息广播给所有的消费者，接收到消息的消费者都可以处理此消息。

消费者组（consumer group）
	消费者组相当于多个消费者充当一个消费者的角色，对于一条数据，多个消费者组，每个都可以读到一份相同的数据。
	在消费者组内部，一条数据只会由其中一个消费者来处理。

假设所有消费者都放在一个消费者组里，那么就实现了队列模型。
假设所有消费者都放在不同的消费者组里，那么久实现了发布-订阅模型。